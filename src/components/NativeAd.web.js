import React from 'react';
import { View } from 'react-native';

// Native Ads are not supported on web version yet
const NativeAdComponent = ({ style }) => {
    return null;
};

export default NativeAdComponent;
